<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>Eldridge's Blog</title>
    <link>https://blog.eldrid.ge/</link>
    <description>Recent content {{ if ne  .Title  .Site.Title }}{{ with .Title }}in {{.}} {{ end }}{{ end }}on {{ .Site.Title }}</description>
    <generator>Hugo -- gohugo.io</generator>{{ with .Site.LanguageCode }}
    <language>{{.}}</language>{{end}}{{ with .Site.Author.email }}
    <managingEditor>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</managingEditor>{{end}}{{ with .Site.Author.email }}
    <webMaster>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</webMaster>{{end}}{{ with .Site.Copyright }}
    <copyright>{{.}}</copyright>{{end}}{{ if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    {{ with .OutputFormats.Get "RSS" }}
        {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{ end }}
    {{ range .Pages }}
    <entry>
      <published>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</published>
      <title>{{ .Title }}</title>
      <content type="html">
        <figure> <img src="https://blog.eldrid.ge{{ .Params.hero | safeHTML }}" />
        </figure>
        {{- .Content | html -}}
      </content>
      <link rel="alternate" type="text/html" href="https://blog.eldrid.ge{{ .Permalink }}"/>
      <id>
        https://blog.eldrid.ge{{ .Permalink }}
      </id>
      <author>
        <name>{{ with .Site.Author.name }}</name>
      </author>
    </entry>
    {{ end }}
  </channel>
</rss>
